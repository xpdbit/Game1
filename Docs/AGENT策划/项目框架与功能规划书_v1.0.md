# 桌面旅者 - 项目框架与功能规划书 v1.0

**文档版本：** 1.0
**更新日期：** 2026-04-23
**基于：** 玩法策划案 v1.1
**状态：** 完成

---

## 一、项目概述

### 1.1 项目定位

**桌面旅者**是一款Unity 6开发的挂机放置游戏，核心差异化为：
- 透明悬浮窗：UniWindowController + D3D11，桌面融合体验
- 虚实结合：键盘/鼠标输入转化为游戏内脚程
- 分支叙事：Roguelike事件树，多结局体验
- 延迟PVP：类似皇室战争的异步对战

### 1.2 技术栈

| 组件 | 版本 | 用途 |
|------|------|------|
| Unity | 6000.1.5f1 | 引擎 |
| URP | 17.1.0 | 渲染管线 |
| C# | 9.0 | 脚本语言 |
| .NET | 4.7.1 | .NET Framework |
| Input System | 1.14.0 | 新输入系统 |
| UniWindowController | 0.9.8 | 透明悬浮窗 |
| TextMeshPro | (bundled) | UI文本 |

### 1.3 项目结构

```
Assets/
├── Scripts/
│   ├── Core/                    # 核心系统
│   │   ├── GameLoop/            # 游戏循环
│   │   │   ├── GameLoopManager.cs
│   │   │   └── IGameLoop.cs
│   │   ├── SaveSystem/          # 存档系统
│   │   │   ├── SaveManager.cs
│   │   │   └── SaveData.cs
│   │   ├── EventBus/            # 事件总线
│   │   │   ├── EventBus.cs
│   │   │   └── EventBase.cs
│   │   ├── Input/               # 输入管理
│   │   │   ├── BackgroundInputManager.cs
│   │   │   └── InputConverter.cs      # [NEW] 虚实交互输入转换
│   │   ├── Debug/               # 调试系统
│   │   │   └── GameDebug.cs
│   │   └── Utils/               # 工具类
│   │       ├── ResourceManager.cs
│   │       ├── Utils.cs
│   │       └── Constants.cs
│   │
│   ├── Modules/                 # 游戏模块
│   │   ├── Idle/                # 挂机收益
│   │   │   ├── IdleRewardModule.cs
│   │   │   └── BonusMultiplierModule.cs
│   │   ├── Travel/               # 旅行系统
│   │   │   ├── TravelManager.cs
│   │   │   ├── ProgressManager.cs
│   │   │   └── TravelState.cs
│   │   ├── Combat/               # 战斗系统
│   │   │   ├── CombatSystem.cs
│   │   │   ├── CombatCalculator.cs  # [NEW] 伤害计算
│   │   │   └── CombatEvent.cs
│   │   ├── Team/                 # 队伍系统
│   │   │   ├── TeamDesign.cs
│   │   │   ├── TeamManager.cs
│   │   │   ├── TeamMemberData.cs
│   │   │   └── [NEW] JobSystem.cs   # 职业系统
│   │   ├── Inventory/            # 背包系统
│   │   │   ├── InventoryDesign.cs
│   │   │   ├── ItemManager.cs
│   │   │   ├── ItemTemplate.cs
│   │   │   ├── ItemInstance.cs
│   │   │   └── [NEW] EquipmentSystem.cs # 装备系统
│   │   ├── Skill/                 # [NEW] 技能系统
│   │   │   ├── SkillDesign.cs
│   │   │   ├── SkillManager.cs
│   │   │   ├── SkillData.cs
│   │   │   └── PassiveSkillEffect.cs
│   │   └── Card/                  # [NEW] 卡牌系统
│   │       ├── CardDesign.cs
│   │       ├── CardManager.cs
│   │       ├── CardGachaSystem.cs
│   │       └── CardData.cs
│   │
│   ├── Entities/                 # 实体
│   │   ├── Player/
│   │   │   ├── PlayerActor.cs
│   │   │   ├── PlayerStats.cs
│   │   │   ├── ModuleCollection.cs
│   │   │   └── IModule.cs
│   │   ├── World/
│   │   │   ├── WorldMap.cs
│   │   │   ├── Location.cs
│   │   │   └── MapGenerator.cs
│   │   └── NPC/
│   │       ├── NPCSystem.cs
│   │       ├── NPCTemplate.cs
│   │       └── NPCInstance.cs
│   │
│   ├── Events/                  # 事件系统
│   │   ├── EventQueue/
│   │   │   ├── EventQueue.cs
│   │   │   ├── IGameEvent.cs
│   │   │   ├── EventResult.cs
│   │   │   ├── CombatEvent.cs
│   │   │   ├── TradeEvent.cs
│   │   │   ├── DiscoveryEvent.cs
│   │   │   └── MysteryEvent.cs
│   │   ├── EventChain/
│   │   │   ├── EventChain.cs
│   │   │   ├── EventChainNode.cs
│   │   │   └── EventChoice.cs
│   │   ├── EventTree/             # [NEED_COMPLETE]
│   │   │   ├── EventTreeManager.cs
│   │   │   ├── EventTreeRunner.cs   # [TODO] 完成实现
│   │   │   ├── EventTreeTemplate.cs
│   │   │   └── EventTreeState.cs
│   │   └── EventManager.cs
│   │
│   ├── UI/                      # UI系统
│   │   ├── UIManager.cs
│   │   ├── UIState.cs
│   │   ├── BaseUIPanel.cs
│   │   ├── IUIPanel.cs
│   │   ├── GameHUDPanel.cs
│   │   ├── UIInventory/
│   │   │   ├── UIInventory.cs
│   │   │   └── UIInventoryItem.cs
│   │   ├── UITeam/
│   │   │   ├── UITeam.cs
│   │   │   └── UITeamMember.cs
│   │   ├── UICard/               # [NEW] 卡牌UI
│   │   │   ├── UICardCollection.cs
│   │   │   └── UIGacha.cs
│   │   ├── Dialog/
│   │   │   ├── UISelectionDialog.cs
│   │   │   └── EventChoiceDialog.cs
│   │   ├── Map/
│   │   │   └── UIMapPath.cs
│   │   └── Utils/
│   │       ├── UIProgressBar.cs
│   │       ├── UIText.cs
│   │       ├── UILayout.cs
│   │       ├── UIListItems.cs
│   │       └── XObjectPool.cs
│   │
│   ├── PVP/                      # [NEW] 延迟PVP系统
│   │   ├── PVPMatchManager.cs
│   │   ├── PVPArenaManager.cs
│   │   ├── PVPSetupPhase.cs
│   │   └── PVPBattlePhase.cs
│   │
│   ├── Prestige/                 # [NEW] 轮回系统
│   │   ├── PrestigeManager.cs
│   │   └── PrestigeReward.cs
│   │
│   ├── GameMain.cs               # 游戏入口
│   └── GameTest.cs               # 测试类
│
├── Resources/
│   └── Data/
│       ├── Items/Items.xml
│       ├── Events/Events.xml
│       └── EventTrees/EventTrees.xml
│
└── Scenes/
    └── SampleScene.unity
```

---

## 二、核心系统设计

### 2.1 游戏循环系统 (Core/GameLoop)

**职责：**
- 管理游戏主循环（Tick/FixedTick/LateTick）
- 协调各子系统更新顺序
- 控制时间缩放和暂停

**接口定义：**
```csharp
public interface IGameLoop
{
    void Tick(float deltaTime);      // 主循环更新
    void FixedTick(float deltaTime);  // 物理更新
    void LateTick(float deltaTime);   // 相机更新后
    int order { get; }               // 更新顺序
}
```

**实现类：**
- `GameLoopManager` - 单例，管理所有IGameLoop实现

### 2.2 存档系统 (Core/SaveSystem)

**职责：**
- JSON序列化和反序列化
- 自动存档和手动存档
- 存档路径管理

**SaveData结构：**
```csharp
[Serializable]
public class SaveData
{
    public PlayerSaveData player;
    public TravelSaveData travel;
    public InventorySaveData inventory;
    public TeamSaveData team;
    public int prestigeCount;
    public long timestamp;
}
```

**API：**
```csharp
public class SaveManager
{
    static SaveManager instance;
    void Save(SaveData data);
    SaveData Load();
    void AutoSave();
    string GetSavePath();
}
```

### 2.3 事件总线 (Core/EventBus)

**职责：**
- 发布-订阅模式解耦系统
- 类型安全的泛型事件

**使用示例：**
```csharp
// 发布
EventBus.instance.Publish(new InventoryChangedEvent(data));

// 订阅
EventBus.instance.Subscribe<InventoryChangedEvent>(OnInventoryChanged);
```

### 2.4 输入管理系统 (Core/Input)

**BackgroundInputManager职责：**
- 监听Keyboard.current和Mouse.current
- 转发输入事件到游戏逻辑

**InputConverter [NEW] 职责：**
- 将键盘敲击转换为脚程
- 将鼠标移动转换为校准判定
- 计算连击加成

**算法设计：**

```csharp
public class InputConverter
{
    // 配置常量
    private const int KEYSTROKES_PER_STEP = 10;      // 每10次敲击 = 1秒脚程
    private const float MOUSE_PIXELS_PER_STEP = 100f; // 每100px = 0.1秒脚程
    private const float COMBO_WINDOW = 1.0f;         // 连击窗口1秒
    private const float MAX_COMBO_MULTIPLIER = 1.5f;  // 最大连击加成
    private const float CALIBRATION_THRESHOLD = 0.8f;  // 校准阈值

    // 状态
    private int _keystrokeCount;
    private float _lastKeystrokeTime;
    private float _comboMultiplier = 1.0f;
    private Vector2 _lastMousePosition;
    private float _lastMouseMoveTime;

    /// <summary>
    /// 将键盘敲击转换为脚程
    /// </summary>
    /// <param name="keystrokeCount">敲击次数</param>
    /// <returns>获得的脚程（秒）</returns>
    public float ConvertKeystrokeToSteps(int keystrokeCount)
    {
        float baseSteps = keystrokeCount / (float)KEYSTROKES_PER_STEP;

        // 更新连击
        UpdateCombo();

        return baseSteps * _comboMultiplier;
    }

    /// <summary>
    /// 将鼠标移动转换为脚程
    /// </summary>
    /// <param name="delta">鼠标移动向量</param>
    /// <returns>获得的脚程（秒）</returns>
    public float ConvertMouseMovementToSteps(Vector2 delta)
    {
        float distance = delta.magnitude;
        float steps = distance / MOUSE_PIXELS_PER_STEP * 0.1f;

        // 精准校准判定：静止>2秒后移动 = 2x加成
        if (IsPrecisionCalibration())
        {
            steps *= 2.0f;
        }

        return steps;
    }

    /// <summary>
    /// 计算校准加成
    /// </summary>
    /// <param name="currentDirection">当前移动方向</param>
    /// <param name="targetDirection">目标方向</param>
    /// <returns>校准加成（成功返回>1.0）</returns>
    public float CalculateCalibrationBonus(Vector2 currentDirection, Vector2 targetDirection)
    {
        if (currentDirection.magnitude < 0.01f || targetDirection.magnitude < 0.01f)
            return 1.0f;

        // 计算方向点积（-1到1）
        float dot = Vector2.Dot(currentDirection.normalized, targetDirection.normalized);

        // 点积>阈值时成功
        if (dot > CALIBRATION_THRESHOLD)
        {
            // 成功：1.0 ~ 1.5 加成
            return 1.0f + (dot - CALIBRATION_THRESHOLD) / (1 - CALIBRATION_THRESHOLD) * 0.5f;
        }

        // 失败：方向偏移惩罚（最多-30%）
        return 1.0f + dot * 0.3f;
    }

    private void UpdateCombo()
    {
        float now = Time.time;
        if (now - _lastKeystrokeTime < COMBO_WINDOW)
        {
            _keystrokeCount++;
            // 连击加成：每10次连击+0.1，最高1.5
            _comboMultiplier = Mathf.Min(MAX_COMBO_MULTIPLIER, 1.0f + _keystrokeCount / 100f);
        }
        else
        {
            _keystrokeCount = 0;
            _comboMultiplier = 1.0f;
        }
        _lastKeystrokeTime = now;
    }

    private bool IsPrecisionCalibration()
    {
        float now = Time.time;
        return (now - _lastMouseMoveTime) > 2.0f;
    }
}
```

---

## 三、游戏模块设计

### 3.1 挂机收益模块 (Modules/Idle)

**IdleRewardModule：**
```csharp
public class IdleRewardModule : IModule
{
    float CalculateOfflineReward(float offlineSeconds);
    void ApplyReward();
    string GetBonus(string bonusType);
}
```

**BonusMultiplierModule：**
- 管理各种加成来源（装备/技能/事件）
- 提供统一加成计算接口

### 3.2 旅行系统模块 (Modules/Travel)

**TravelManager：**
```csharp
public class TravelManager
{
    // 状态管理
    void StartNewJourney(string seed);
    void StartTravel(string fromLocationId, string toLocationId);
    void CancelTravel();

    // 进度
    void UpdateProgress(float deltaTime);
    float GetProgress(); // 0~1

    // 事件
    void OnTravelCompleted();
    void TriggerRandomEvent();
}
```

**ProgressManager：**
```csharp
public class ProgressManager
{
    float travelPoints;      // 当前旅行点
    float travelRate;        // TP/s
    float pointsPerEvent;    // 触发事件的阈值(200)
    float pointsPerTree;     // 触发事件树的阈值(1000)

    void AddPoints(float points);
    bool ShouldTriggerEvent();
    bool ShouldTriggerEventTree();
}
```

**TravelState枚举：**
```csharp
public enum TravelStatus
{
    Idle,           // 空闲
    Traveling,     // 旅行中
    AwaitingChoice, // 等待分支选择
    EventActive,    // 事件进行中
    ProgressMilestone // 进度里程碑
}
```

### 3.3 战斗系统模块 (Modules/Combat)

**CombatSystem：**
```csharp
public class CombatSystem
{
    CombatResult ExecuteCombat(CombatParams @params);
    int CalculateDamage(AttackParams @params);
    bool CanVictory(TeamMemberData[] team, EnemyData[] enemies);
}

public struct CombatParams
{
    public TeamMemberData[] attackers;
    public EnemyData[] defenders;
    public bool isPlayerAttacking;
}

public enum CombatResult
{
    Victory,
    Defeat,
    Draw
}
```

**CombatCalculator [NEW]：**
```csharp
public static class CombatCalculator
{
    // 伤害计算公式
    public static int CalculateDamage(int attack, int defense, float skillMultiplier = 1.0f)
    {
        int baseDamage = Mathf.Max(1, attack - defense / 2);
        return (int)(baseDamage * skillMultiplier);
    }

    // 暴击伤害
    public static int CalculateCritDamage(int attack, float critMultiplier = 2.0f)
    {
        return (int)(attack * critMultiplier);
    }

    // 暴击率
    public static float CalculateCritRate(int wisdom, float equipmentBonus = 0)
    {
        return Mathf.Min(0.5f, wisdom / 100f + equipmentBonus);
    }

    // 伤害减免
    public static float CalculateDamageReduction(int defense)
    {
        return defense / (defense + 100f);
    }

    // 实际受到伤害
    public static int CalculateActualDamage(int enemyDamage, int defense)
    {
        float reduction = CalculateDamageReduction(defense);
        return (int)(enemyDamage * (1 - reduction));
    }
}
```

### 3.4 队伍系统模块 (Modules/Team)

**TeamDesign：**
```csharp
public class TeamDesign
{
    IReadOnlyList<TeamMemberData> members { get; }
    int memberCount { get; }
    TeamCapacity capacity { get; set; }

    TeamOperationResult AddMember(TeamMemberData member);
    TeamOperationResult RemoveMember(int memberId);
    TeamOperationResult UpdateMember(TeamMemberData member);
    int GetTotalCombatPower();

    event Action<TeamEventData> onTeamChanged;
}
```

**TeamMemberData：**
```csharp
[Serializable]
public class TeamMemberData
{
    public int id;
    public string name;
    public int level;

    // 属性
    public int maxHp;
    public int currentHp;
    public int attack;
    public int defense;
    public float speed;

    // 附加属性
    public int charisma;  // 魅力
    public int wisdom;    // 智慧

    // 装备
    public string weaponTemplateId;
    public string armorTemplateId;
    public string accessory1TemplateId;
    public string accessory2TemplateId;
    public string mountTemplateId;

    // [NEW] 职业
    public JobType job;

    // 技能列表
    public List<string> passiveSkillIds;
    public List<string> activeSkillIds;
    public string ultimateSkillId;

    // 计算属性
    public int GetTotalAttack();
    public int GetTotalDefense();
    public float GetSpeed();
    public bool IsAlive();
    public float GetHpPercent();
    public int GetCombatPower();
}
```

**JobSystem [NEW] 职业系统：**
```csharp
public enum JobType
{
    None,   // 无职业（主角）
    Merchant,  // 商贾
    Escort,    // 镖师
    Scholar,    // 学者
    Healer     // 医者
}

public class JobSystem
{
    // 职业属性加成
    int GetJobAttributeBonus(JobType job, AttributeType attr);

    // 职业专属技能
    string GetJobDefaultPassiveSkill(JobType job);
    string GetJobDefaultActiveSkill(JobType job);

    // 职业特效
    float GetTradeBonus(JobType job);        // 商贾
    float GetCombatBonus(JobType job);       // 镖师
    float GetDiscoveryBonus(JobType job);    // 学者
    float GetHealBonus(JobType job);         // 医者
}
```

### 3.5 背包系统模块 (Modules/Inventory)

**InventoryDesign：**
```csharp
public class InventoryDesign
{
    // 容量
    int maxSlotCount;
    float maxWeight;

    // 核心操作
    InventoryOperationResult AddItem(string templateId, int amount);
    InventoryOperationResult RemoveItem(int instanceId, int amount);
    ItemInstance GetItem(int instanceId);
    List<ItemInstance> GetItemsByTemplateId(string templateId);
    List<ItemInstance> GetItemsByType(ItemType type);

    // 查询
    bool CanAddItem(string templateId, int amount);
    float GetTotalWeight();
    int GetInventoryCount();

    // 事件
    event Action<InventoryEventData> onInventoryChanged;
}
```

**ItemTemplate：**
```csharp
[Serializable]
public class ItemTemplate
{
    public string id;              // "Core.Item.Bacon"
    public string nameTextId;
    public string descTextId;
    public ItemType type;
    public float weight;
    public int maxStack = 99;

    // 类型特定属性
    public float foodCalorific;   // 食物热量
    public float fuelCalorific;    // 燃料热值
    public int damage;             // 武器伤害
    public int armor;              // 防具防御
    public float moveSpeedOffset;  // 速度偏移
    public float moveSpeedFactor;  // 速度倍率
}
```

**EquipmentSystem [NEW] 装备系统：**
```csharp
public class EquipmentSystem
{
    // 装备操作
    InventoryOperationResult Equip(TeamMemberData member, ItemInstance item, EquipmentSlot slot);
    ItemInstance Unequip(TeamMemberData member, EquipmentSlot slot);

    // 强化
    InventoryOperationResult EnhanceEquipment(ItemInstance equipment, int level);
    int GetEnhanceCost(ItemTemplate template, int currentLevel);

    // 属性计算
    int GetEquipmentDamage(ItemInstance equipment);
    int GetEquipmentDefense(ItemInstance equipment);
    float GetEquipmentSpeedPenalty(ItemTemplate template);
}

public enum EquipmentSlot
{
    Weapon,
    Armor,
    Accessory1,
    Accessory2,
    Mount
}
```

### 3.6 技能系统模块 (Modules/Skill) [NEW]

**SkillDesign：**
```csharp
public class SkillDesign
{
    static SkillDesign instance;

    // 技能查询
    SkillData GetSkill(string skillId);
    bool HasSkill(string skillId);
    List<string> GetAvailableSkills(TeamMemberData member);

    // 技能效果
    void ApplyPassiveSkill(TeamMemberData member, string skillId);
    SkillResult ExecuteActiveSkill(string skillId, TeamMemberData caster, CombatTarget target);
    void ExecutePassiveEffect(string skillId, TeamMemberData member);
}

[Serializable]
public class SkillData
{
    public string id;
    public string nameTextId;
    public SkillType type;          // Passive, Active, Ultimate
    public int maxLevel;
    public int currentLevel;

    // 效果数值
    public string effectType;       // "damage_boost", "heal", "trade_bonus"...
    public float effectValue;
    public float cooldown;          // 主动技能CD(秒)
    public int manaCost;            // 消耗(如果有)

    // 条件
    public SkillCondition condition;
}

public enum SkillType
{
    Passive,   // 被动技能
    Active,   // 主动技能
    Ultimate  // 终极技能
}
```

### 3.7 卡牌系统模块 (Modules/Card) [NEW]

**CardDesign：**
```csharp
public class CardDesign
{
    static CardDesign instance;

    // 卡牌收藏
    List<CardData> GetAllCards();
    List<CardData> GetCardsByRarity(CardRarity rarity);
    int GetCardCount(CardRarity rarity);

    // 抽卡
    GachaResult DrawCards(GachaType type, int count);
    bool IsGuaranteedSSR(GachaType type);

    // 卡牌激活
    void ActivateCard(string cardId);
    void DeactivateCard(string cardId);
}

[Serializable]
public class CardData
{
    public string id;
    public CardType type;          // Character, Equipment, Skill, Item, Event
    public CardRarity rarity;       // N, R, SR, SSR, UR, GR
    public string nameTextId;

    // 属性倍率
    public float attributeMultiplier;

    // 附加词缀
    public List<string> affixIds;

    // 激活状态
    public bool isActivated;
}

public enum GachaType
{
    Novice,     // 新手包
    Standard,   // 标准包
    Premium,    // 高级包
    Limited     // 限定包
}

public struct GachaResult
{
    public bool success;
    public List<CardData> cards;
    public bool isGuaranteed;      // 保底触发
    public int pityCounter;         // 保底计数
}
```

---

## 四、事件系统设计

### 4.1 即时事件 (Events/EventQueue)

**EventQueue：**
```csharp
public class EventQueue
{
    int pendingCount { get; }
    bool hasCurrentEvent { get; }
    IGameEvent currentEvent { get; }

    void Enqueue(IGameEvent gameEvent);
    EventResult ProcessNext();
    IGameEvent PeekNext();
    void Clear();
    void GeneratePendingEvents(float offlineTime);
    void Tick(float deltaTime);
    List<IGameEvent> GetHistory();

    event Action<IGameEvent> onEventTriggered;
    event Action<EventResult> onEventCompleted;
}
```

**IGameEvent接口：**
```csharp
public interface IGameEvent
{
    string eventId { get; }
    string title { get; }
    string description { get; }
    GameEventType eventType { get; }
    bool CanTrigger();
    EventResult Execute();
}
```

**EventResult：**
```csharp
public class EventResult
{
    public bool success;
    public int goldReward;
    public int goldCost;
    public int expReward;
    public List<string> unlockedModuleIds;
    public List<string> removedModuleIds;
    public List<ItemReward> itemRewards;
    public string message;
    public bool isGameOver;
    public string triggerChainId;   // 触发事件链
}
```

### 4.2 分支叙事 (Events/EventTree)

**EventTreeManager：**
```csharp
public class EventTreeManager
{
    void LoadTree(string treeId);
    EventTreeTemplate GetTree(string treeId);
    bool HasTree(string treeId);
    List<string> GetAllTreeIds();
}
```

**EventTreeRunner [NEED_COMPLETE]：**
```csharp
public class EventTreeRunner
{
    // 状态
    EventTreeState state { get; }
    EventTreeTemplate currentTree { get; }
    int currentNodeIndex { get; }
    List<int> choiceHistory { get; }

    // 控制
    void StartTree(EventTreeTemplate tree);
    void SelectChoice(int choiceIndex);
    void GoBack();                 // 返回上一步
    void SkipToEnd();               // 跳过到结局
    void EndTree();

    // 查询
    EventTreeNode GetCurrentNode();
    List<EventChoice> GetCurrentChoices();
    float GetProgress();

    event Action<EventTreeNode> onNodeChanged;
    event Action<EventResult> onTreeCompleted;
}

public enum EventTreeState
{
    NotRunning,
    Running,
    Paused,
    Completed
}

[Serializable]
public class EventTreeTemplate
{
    public string id;
    public string titleTextId;
    public List<EventTreeNode> nodes;
    public int rootNodeIndex;
    public List<int> endingNodeIndices;
}

[Serializable]
public class EventTreeNode
{
    public int index;
    public string titleTextId;
    public string descriptionTextId;
    public EventTreeNodeType nodeType;  // Root, Choice, Random, End
    public List<EventChoice> choices;
    public int nextNodeIndex;            // 无分支时的下一节点
    public EventResult endResult;        // 结局结果
}

[Serializable]
public class EventChoice
{
    public int index;
    public string textTextId;
    public int nextNodeIndex;
    public EventResult result;          // 选择结果
    public List<ChoiceRequirement> requirements;
}

public enum EventTreeNodeType
{
    Root,
    Choice,
    Random,
    End
}
```

---

## 五、UI系统设计

### 5.1 UI架构

**UIManager：**
```csharp
public class UIManager
{
    static UIManager instance;

    // 面板管理
    void OpenPanel(UIState panelId);
    void ClosePanel(UIState panelId);
    void CloseAllPanels();
    bool IsPanelOpen(UIState panelId);

    // 状态切换
    void ChangeState(UIState newState);
    UIState currentState { get; }

    // 提示
    void ShowToast(string message, float duration = 2f);
    void ShowConfirmDialog(string title, string message, Action onConfirm, Action onCancel = null);
}

public enum UIState
{
    None,
    Main,
    Inventory,
    Team,
    Card,
    Settings,
    Map,
    Event,
    Combat,
    PVP,
    Gacha
}
```

**BaseUIPanel：**
```csharp
public abstract class BaseUIPanel : MonoBehaviour, IUIPanel
{
    public UIState panelId;
    public bool isOpen { get; protected set; }

    public virtual void OnOpen();
    public virtual void OnClose();
    public virtual void OnUpdate();
}
```

### 5.2 UI组件

**UIInventory：**
- 背包列表显示
- 物品分类筛选
- 物品使用/装备/出售

**UITeam：**
- 队伍成员列表
- 成员属性显示
- 职业标签显示
- 升级/替换操作

**UICardCollection [NEW]：**
- 卡牌收藏展示
- 稀有度筛选
- 卡牌激活/停用

**UIGacha [NEW]：**
- 抽卡界面
- 卡包选择
- 抽卡动画
- 结果展示

**UISelectionDialog：**
- 事件选项显示
- 结果展示

**UIMapPath：**
- 节点地图可视化
- 当前进度显示
- 节点点击信息

---

## 六、延迟PVP系统 [NEW]

### 6.1 架构

**PVPMatchManager：**
```csharp
public class PVPMatchManager
{
    static PVPMatchManager instance;

    // 匹配
    void StartMatch();
    void CancelMatch();
    MatchStatus GetMatchStatus();
    float GetMatchProgress();  // 0~1
    float estimatedWaitTime;

    // 对战
    void StartPVP(string opponentId);
    PVPArenaManager currentArena { get; }

    event Action<MatchResult> onMatchCompleted;
}

public enum MatchStatus
{
    Idle,
    Searching,
    Found,
    Starting
}
```

**PVPArenaManager：**
```csharp
public class PVPArenaManager
{
    // 布阵阶段(30秒)
    void PlaceUnit(CardData card, LanePosition position);
    void RemoveUnit(LanePosition position);
    List<PlacedUnit> GetPlacedUnits();
    void ConfirmDeployment();

    // 对战阶段(60秒)
    void StartBattle();
    LaneResult GetLaneResult(LanePosition position);
    MatchResult GetMatchResult();

    // 状态
    PVPArenaState state { get; }
    float remainingTime;

    event Action<LaneResult> onLaneResolved;
    event Action<MatchResult> onArenaCompleted;
}

public enum PVPArenaState
{
    Setup,      // 布阵期
    Battle,     // 对战期
    Result      // 结算期
}

public struct LanePosition
{
    public int laneIndex;  // 0-2 (三条路)
    public bool isLeft;    // 左边/右边
}

public struct PlacedUnit
{
    public CardData card;
    public LanePosition position;
    public float moveSpeed;
    public float attackRange;
    public int damage;
}
```

---

## 七、轮回系统 [NEW]

### 7.1 PrestigeManager

```csharp
public class PrestigeManager
{
    static PrestigeManager instance;

    // 轮回查询
    int prestigeCount { get; }
    int prestigePoints { get; }
    float GetRetentionRate();  // 资源保留率

    // 轮回操作
    bool CanPrestige();
    PrestigeResult ExecutePrestige();

    // 轮回商店
    void PurchasePrestigeUpgrade(string upgradeId);
    List<PrestigeUpgrade> GetAvailableUpgrades();

    event Action<PrestigeResult> onPrestigeCompleted;
}

public class PrestigeResult
{
    public int pointsEarned;
    public float retainedGold;
    public float retainedExpPercent;
    public List<string> retainedSkills;
    public int newPrestigeCount;
}

[Serializable]
public class PrestigeUpgrade
{
    public string id;
    public string nameTextId;
    public int cost;
    public PrestigeUpgradeType type;
    public float effectValue;
    public bool isPurchased;
}

public enum PrestigeUpgradeType
{
    ResourceRetention,   // 资源保留+
    PrestigePointBonus,  // 轮回点数加成
    StartingEquipment,    // 初始装备
    SkillUnlock,          // 技能解锁
    UniqueFeature         // 独特功能
}
```

---

## 八、数据结构总览

### 8.1 玩家数据

```csharp
[Serializable]
public class PlayerSaveData
{
    public string id;
    public string actorName;
    public int level;
    public int exp;
    public int maxHp;
    public int currentHp;
    public int attack;
    public int defense;
    public float speed;
    public int charisma;
    public int wisdom;
    public int gold;
    public int prestigePoints;
    public int prestigeCount;
}
```

### 8.2 旅行数据

```csharp
[Serializable]
public class TravelSaveData
{
    public string mapSeed;
    public int currentNodeIndex;
    public int maxNodeIndex;
    public TravelStatus status;
    public float progress;
    public float travelPoints;
    public long lastUpdateTime;
}
```

### 8.3 物品数据

```csharp
[Serializable]
public class ItemInstance
{
    public int instanceId;
    public string templateId;
    public int amount;
}

[Serializable]
public class InventorySaveData
{
    public List<ItemInstance> items;
    public int maxSlotCount;
    public float maxWeight;
}
```

### 8.4 队伍数据

```csharp
[Serializable]
public class TeamSaveData
{
    public List<TeamMemberData> members;
    public int maxTeamSize;
}
```

### 8.5 卡牌数据

```csharp
[Serializable]
public class CardCollectionSaveData
{
    public List<CardData> ownedCards;
    public List<string> activatedCardIds;
    public Dictionary<GachaType, int> pityCounters;
}
```

---

## 九、实现顺序与依赖

### Phase 1：核心玩法（MVP）

| 顺序 | 模块 | 依赖 | 工作量 | 优先级 |
|------|------|------|--------|--------|
| 1 | 游戏循环系统 | 无 | 低 | P0 |
| 2 | 存档系统 | 游戏循环 | 中 | P0 |
| 3 | 事件总线 | 游戏循环 | 低 | P0 |
| 4 | 背包系统 | 事件总线,存档 | 中 | P0 |
| 5 | 旅行系统 | 背包,存档 | 高 | P0 |
| 6 | 事件队列 | 旅行系统 | 高 | P0 |
| 7 | 基础UI | 上述所有 | 高 | P0 |
| 8 | 挂机收益 | 事件队列 | 中 | P0 |

### Phase 2：成长系统

| 顺序 | 模块 | 依赖 | 工作量 | 优先级 |
|------|------|------|--------|--------|
| 9 | 战斗系统 | 旅行系统 | 高 | P1 |
| 10 | 队伍系统 | 背包系统 | 中 | P1 |
| 11 | 角色升级 | 战斗系统 | 中 | P1 |
| 12 | 装备系统 | 队伍系统 | 高 | P1 |

### Phase 3：深度玩法

| 顺序 | 模块 | 依赖 | 工作量 | 优先级 |
|------|------|------|--------|--------|
| 13 | 技能系统 | 装备系统 | 高 | P2 |
| 14 | 事件树 | 事件队列 | 高 | P1 |
| 15 | 分支叙事UI | 事件树 | 中 | P1 |
| 16 | 虚实交互输入 | 游戏循环 | 中 | P2 |
| 17 | 职业系统 | 队伍系统 | 中 | P2 |

### Phase 4：商业化

| 顺序 | 模块 | 依赖 | 工作量 | 优先级 |
|------|------|------|--------|--------|
| 18 | 卡牌系统 | 技能系统 | 高 | P2 |
| 19 | 抽卡UI | 卡牌系统 | 中 | P2 |
| 20 | 轮回系统 | 卡牌系统 | 高 | P2 |
| 21 | 延迟PVP | 队伍,卡牌 | 极高 | P2 |

---

## 十、XML配置格式

### 10.1 物品配置 (Items.xml)

```xml
<?xml version="1.0" encoding="UTF-8"?>
<Items>
    <Item>
        <id>Core.Item.GoldCoin</id>
        <nameTextId>Core.Item.GoldCoin.NameText</nameTextId>
        <descTextId>Core.Item.GoldCoin.DescriptionText</descTextId>
        <type>Money</type>
        <weight>0</weight>
        <maxStack>9999</maxStack>
    </Item>
    <Item>
        <id>Core.Item.Bacon</id>
        <nameTextId>Core.Item.Bacon.NameText</nameTextId>
        <descTextId>Core.Item.Bacon.DescriptionText</descTextId>
        <type>Food</type>
        <weight>0.5</weight>
        <foodCalorific>1200</foodCalorific>
        <maxStack>99</maxStack>
    </Item>
    <Item>
        <id>Core.Item.ShortBlade</id>
        <nameTextId>Core.Item.ShortBlade.NameText</nameTextId>
        <descTextId>Core.Item.ShortBlade.DescriptionText</descTextId>
        <type>Weapon</type>
        <weight>2.0</weight>
        <damage>5</damage>
        <maxStack>1</maxStack>
    </Item>
</Items>
```

> **注意**：物品ID统一使用 `Core.Item.{Name}` 格式，与ItemList.md保持一致

### 10.2 事件配置 (Events.xml)

```xml
<?xml version="1.0" encoding="UTF-8"?>
<Events>
    <Event>
        <id>combat_001</id>
        <type>Combat</type>
        <enemyCount base="2" variance="2"/>
        <enemyStrength base="20" variance="30"/>
        <rewards>
            <base>20</base>
            <perEnemy>15</perEnemy>
            <strengthMultiplier>true</strengthMultiplier>
        </rewards>
    </Event>
    <Event>
        <id>trade_001</id>
        <type>Trade</type>
        <tradeCost>30</tradeCost>
        <tradeBonus>0.2</tradeBonus>
    </Event>
</Events>
```

### 10.3 事件树配置 (EventTrees.xml)

```xml
<?xml version="1.0" encoding="UTF-8"?>
<EventTrees>
    <EventTree>
        <id>tutorial_01</id>
        <titleTextId>EventTree.Tutorial01.Title</titleTextId>
        <nodes>
            <Node>
                <index>0</index>
                <type>Root</type>
                <titleTextId>EventTree.Tutorial01.Node0.Title</titleTextId>
                <descriptionTextId>EventTree.Tutorial01.Node0.Description</descriptionTextId>
                <choices>
                    <Choice>
                        <index>0</index>
                        <textTextId>EventTree.Tutorial01.Node0.Choice0</textTextId>
                        <nextNode>1</nextNode>
                    </Choice>
                    <Choice>
                        <index>1</index>
                        <textTextId>EventTree.Tutorial01.Node0.Choice1</textTextId>
                        <nextNode>2</nextNode>
                    </Choice>
                </choices>
            </Node>
            <Node>
                <index>1</index>
                <type>Choice</type>
                <titleTextId>EventTree.Tutorial01.Node1.Title</titleTextId>
                <descriptionTextId>EventTree.Tutorial01.Node1.Description</descriptionTextId>
                <choices>
                    <Choice>
                        <index>0</index>
                        <textTextId>EventTree.Tutorial01.Node1.Choice0</textTextId>
                        <nextNode>5</nextNode>
                        <result>
                            <goldReward>50</goldReward>
                            <message>EventTree.Tutorial01.Node1.Result0</message>
                        </result>
                    </Choice>
                </choices>
            </Node>
            <Node>
                <index>5</index>
                <type>End</type>
                <endResult>
                    <goldReward>100</goldReward>
                    <message>EventTree.Tutorial01.End.Message</message>
                </endResult>
            </Node>
        </nodes>
        <rootNodeIndex>0</rootNodeIndex>
        <endingNodeIndices>5</endingNodeIndices>
    </EventTree>
</EventTrees>
```

### 10.4 技能配置 (Skills.xml) [NEW]

```xml
<?xml version="1.0" encoding="UTF-8"?>
<Skills>
    <Skill>
        <id>Core.Skill.Passive.Merchant</id>
        <nameTextId>Core.Skill.Passive.Merchant.NameText</nameTextId>
        <type>Passive</type>
        <maxLevel>5</maxLevel>
        <effectType>trade_bonus</effectType>
        <effectValue>0.06</effectValue>  <!-- 每级+6% -->
    </Skill>
    <Skill>
        <id>Core.Skill.Active.Sweep</id>
        <nameTextId>Core.Skill.Active.Sweep.NameText</nameTextId>
        <type>Active</type>
        <maxLevel>3</maxLevel>
        <effectType>area_damage</effectType>
        <effectValue>0.5</effectValue>  <!-- 50%伤害 -->
        <cooldown>30</cooldown>
    </Skill>
</Skills>
```

### 10.5 卡牌配置 (Cards.xml) [NEW]

```xml
<?xml version="1.0" encoding="UTF-8"?>
<Cards>
    <Card>
        <id>Core.Card.Character.ZhangFei</id>
        <nameTextId>Core.Card.Character.ZhangFei.NameText</nameTextId>
        <type>Character</type>
        <rarity>SR</rarity>
        <attributeMultiplier>1.5</attributeMultiplier>
        <affixCount>2</affixCount>
    </Card>
    <Card>
        <id>Core.Card.Equipment.LongSword</id>
        <nameTextId>Core.Card.Equipment.LongSword.NameText</nameTextId>
        <type>Equipment</type>
        <rarity>R</rarity>
        <attributeMultiplier>1.2</attributeMultiplier>
        <affixCount>1</affixCount>
    </Card>
</Cards>
```

---

## 十一、命名规范

### 11.1 文件命名
- 类文件：PascalCase.cs（如 `GameMain.cs`）
- 接口文件：I + PascalCase（如 `IGameEvent.cs`）

### 11.2 命名空间
```csharp
namespace Game1
{
    namespace Core { }
    namespace Modules { }
    namespace Entities { }
    namespace Events { }
    namespace UI { }
}
```

### 11.3 ID命名（策划案规范）
- 格式：`{Package}.{Category}.{Name}`（dot分隔）
- 示例：`Core.Item.WaterBottle`
- 扩展：`Core.Item.WaterBottle.NameText`

### 11.4 私有字段
- 格式：`_camelCase`
- 示例：`_members`, `_maxTeamSize`

### 11.5 SerializeField
```csharp
[SerializeField] private int _value;
```

---

## 十二、注意事项

1. **Input System**：使用 `Keyboard.current` 而非 `UnityEngine.Input`
2. **禁止空MonoBehaviour**：每个脚本必须有实际功能
3. **缓存组件引用**：Awake/Start中缓存，Update中不GetComponent
4. **异步加载**：大资源使用 Addressables/async
5. **透明窗口**：需要 D3D11 + FlipModel禁用 + HDR关闭
6. **后台输入**：使用 UniWindowController 静态API

---

*文档结束*

**文档版本历史：**
- v1.0 (2026-04-23)：初始版本，基于玩法策划案v1.1
